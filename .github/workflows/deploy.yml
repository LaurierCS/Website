name: Deploy to Firebase
on:
  push:
    branches:
      - live
  pull_request:
    branches:
      - live

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: latest
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Dependencies
        run: pnpm install

      - name: Build
        env:
          VITE_LCS_FIREBASE_APIKEY: ${{ secrets.VITE_LCS_FIREBASE_APIKEY }}
          VITE_LCS_FIREBASE_AUTHDOMAIN: ${{ secrets.VITE_LCS_FIREBASE_AUTHDOMAIN }}
          VITE_LCS_FIREBASE_DBURL: ${{ secrets.VITE_LCS_FIREBASE_DBURL }}
          VITE_LCS_FIREBASE_PROJECTID: ${{ secrets.VITE_LCS_FIREBASE_PROJECTID }}
          VITE_LCS_FIREBASE_STORAGEBUCKET: ${{ secrets.VITE_LCS_FIREBASE_STORAGEBUCKET }}
          VITE_LCS_FIREBASE_MESSAGINGSNEDERID: ${{ secrets.VITE_LCS_FIREBASE_MESSAGINGSNEDERID }}
          VITE_LCS_FIREBASE_APPID: ${{ secrets.VITE_LCS_FIREBASE_APPID }}
          VITE_LCS_FIREBASE_MEASRUEMENTID: ${{ secrets.VITE_LCS_FIREBASE_MEASRUEMENTID }}
        run: pnpm build

      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          channelId: live
          projectId: lcs-frontpage
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks